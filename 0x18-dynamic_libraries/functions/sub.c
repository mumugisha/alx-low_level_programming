#include <stdlib.h>
#include <stdio.h>

/**
 * sub - subtact two parts integers
 * @a: first integer
 * @b: second integer
 * Return: subtracted numbers
 */
int sub(int a, int b)
{
	return (a - b);
}
